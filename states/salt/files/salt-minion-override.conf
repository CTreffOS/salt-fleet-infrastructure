# salt-minion sometimes has a very bad time connecting to the master, so let's make sure it comes up (at some point) and doesn't just give up after a few tries

# Prevent the service from entering the failed state due to too many restarts
[Unit]
StartLimitBurst=0
StartLimitIntervalSec=0

[Service]
Restart=always
RestartSec=10s
